rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORAL: no desplegar este archivo sin antes copiar y revisar las reglas
    // actualmente publicadas en Firebase para el proyecto de produccion.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
